/**
 * Scheduled Job Run detail page route
 *
 * This is a thin wrapper that renders the main app page.
 * The app/page.tsx handles URL routing internally.
 */

import HomePage from "../../../../page"

export default HomePage
