/**
 * Chat page route with dynamic chatId
 *
 * This is a thin wrapper that renders the main app page.
 * The app/page.tsx handles URL routing internally and reads
 * the chatId from URL params.
 */

import HomePage from "../../page"

export default HomePage
