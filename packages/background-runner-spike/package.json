{
  "name": "@background-agents/background-runner-spike",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Throwaway spike: compare two ways to run and reconnect to long-running sandbox processes — nohup+files (Option C) vs Daytona session API (Option A).",
  "main": "src/index.ts",
  "scripts": {
    "demo": "tsx src/demo.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@daytonaio/sdk": "^0.170.0",
    "@types/node": "^20.10.0",
    "dotenv": "^17.3.1",
    "tsx": "^4.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
